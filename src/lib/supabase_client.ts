// This file serves as a compatibility layer for the analytics system
// It reexports the existing supabase client with the same interface

import { supabase } from './supabase';

export { supabase }; 